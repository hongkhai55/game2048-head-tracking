import controller.GameApplication;
import javafx.application.Application;

/**
 * Entry point — launches JavaFX application.
 */
public class Main {
    public static void main(String[] args) {
        Application.launch(GameApplication.class, args);
    }
}
